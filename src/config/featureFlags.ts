/**
 * Temporary toggles for features that are being reworked. Flip back to `true`
 * to restore the login gate / account details once they're ready again.
 */

export const REQUIRE_LOGIN = false;

export const SHOW_ACCOUNT_DETAILS = false;
